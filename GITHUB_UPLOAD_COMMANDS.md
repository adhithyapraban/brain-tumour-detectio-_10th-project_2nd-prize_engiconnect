# GitHub Upload Commands

After creating an empty repository on GitHub, run these commands inside this folder:

```bash
git init
git add .
git commit -m "Initial commit: brain tumour detection CNN"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/brain-tumour-detection.git
git push -u origin main
```

Replace `YOUR_USERNAME` with your actual GitHub username.
